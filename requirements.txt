# ORAKLE — dictée vocale locale (Python 3.12)

# Cœur Phase 0–1 (MVP push-to-talk FR)
PyQt6>=6.6
faster-whisper>=1.0
sounddevice>=0.4
pynput>=1.7
pyperclip>=1.8
numpy>=1.24

# Couper le son des autres apps pendant la dictée (Windows, WASAPI).
# Optionnel : si absent, le ducking est simplement désactivé (pas de crash).
pycaw>=20240210 ; sys_platform == "win32"

# Phase 5 — traduction offline : voir requirements-translate.txt
# (argos-translate tire torch + spacy + stanza, ~plusieurs centaines de Mo).
# Inutile pour le MVP push-to-talk. À installer le moment venu :
#   pip install -r requirements-translate.txt
